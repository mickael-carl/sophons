- hosts: all
  tasks:
    - ansible.builtin.apt:
        update-cache: true
      register: "hello"
    - ansible.builtin.file:
        path: /register
        state: touch
      when: "hello.cache_updated"
